# =============================================================================
# REQUIREMENTS — Physics Tensor Library
# =============================================================================
# Location:  Repository ROOT (alongside README.md, pyproject.toml, .github/)
# Install:   pip install -r requirements.txt
# Python:    3.10 | 3.11 | 3.12
# =============================================================================

# ------------------------------------------------------------------------------
# CORE RUNTIME
# These packages are imported directly by the library at runtime.
# ------------------------------------------------------------------------------
numpy>=1.24.0,<3.0.0          # ND-array backend, linear algebra, broadcasting
scipy>=1.10.0,<2.0.0          # Sparse solvers, optimize, integrate, special functions
sympy>=1.14.0,<2.0.0            # Symbolic dimensional analysis, unit algebra, simplification
matplotlib>=3.7.0,<4.0.0      # Plotting backend for tensor visualisations & examples

# ------------------------------------------------------------------------------
# TESTING  (consumed by ci.yml)
# ------------------------------------------------------------------------------
pytest>=7.4.0,<10.0.0          # Test runner, fixtures, parametrised tests
pytest-cov>=4.1.0,<6.0.0      # Coverage reports with --cov-fail-under threshold
pytest-xdist>=3.8.0,<4.0.0    # Parallel test execution (-n auto) for faster CI
hypothesis>=6.163.0,<7.0.0     # Property-based testing (fuzz tensor invariants)

# ------------------------------------------------------------------------------
# CODE QUALITY  (consumed by lint.yml)
# ------------------------------------------------------------------------------
black>=26.5.1,<27.0.0         # Uncompromising code formatter
flake8>=7.3.0,<8.0.0          # PEP-8 style & complexity enforcement
mypy>=1.5.0,<2.0.0            # Static type checker with strict mode
isort>=8.0.1,<9.0.0          # Import sorting (black-compatible profile)
pylint>=4.0.6,<5.0.0         # Deep static analysis, docstring & architecture checks

# ------------------------------------------------------------------------------
# BUILD & PUBLISH  (consumed by publish.yml)
# ------------------------------------------------------------------------------
build>=1.5.0,<2.0.0          # PEP-517 builder (replaces python setup.py sdist)
twine>=4.0.0,<6.0.0           # Secure, verified uploads to PyPI
setuptools>=68.0.0,<84.0.0    # Package discovery, entry points, namespace packages
wheel>=0.41.0,<0.50.0         # Binary wheel generation

# ------------------------------------------------------------------------------
# OPTIONAL: DEVELOPMENT EXTRAS
# Uncomment the lines below when you add docs / notebooks / pre-commit.
# ------------------------------------------------------------------------------
# sphinx>=7.0.0,<8.0.0        # Documentation generator
# sphinx-rtd-theme>=1.3.0,<3.0.0   # ReadTheDocs theme
# myst-parser>=2.0.0,<4.0.0     # Markdown support in Sphinx
# jupyter>=1.0.0,<2.0.0         # Interactive notebooks for examples
# pre-commit>=3.3.0,<4.0.0      # Git hooks framework (runs black/flake8 before commit)
