# =============================================================================
# RUNTIME REQUIREMENTS — PhysTensor
# =============================================================================
# These are the packages imported directly by the library at runtime.
# End users get these automatically via `pip install phystensor`.
# Keep this file minimal — dev tools belong in requirements-dev.txt.
# =============================================================================

# ND-array backend, linear algebra, broadcasting
# >=1.26.4 resolves all open CVEs in the 1.24.x/1.25.x line
numpy>=1.26.4,<3.0.0

# Sparse solvers, optimize, integrate, special functions
# Remove the duplicate line that was breaking Dependabot
scipy>=1.14.0,<2.0.0

# Plotting backend for tensor visualisations & examples
matplotlib>=3.9.0,<4.0.0

urllib3>=2.0.7
requests>=2.31.0
pillow>=10.0.1
cryptography>=41.0.4
PyYAML>=6.0.1
certifi>=2023.7.22
