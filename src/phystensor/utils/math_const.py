import numpy as np

# Dimensionless constants (vector is all zeros)
# These are used for EEE phase calcs and Maritime rotations.

PI = np.pi
TAU = 2 * np.pi
E = np.e
GOLDEN_RATIO = (1 + 5**0.5) / 2

# Conversion factors for angles (Dimensionless)
DEG_TO_RAD = PI / 180.0
RAD_TO_DEG = 180.0 / PI
